{
  "name": "flask-electron-app",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . MiAplicacion --platform=win32 --arch=x64 --out=release-build --overwrite"
  },
  "devDependencies": {
    "electron": "^33.2.0"
  }
}
